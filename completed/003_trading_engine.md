Implement trading engine logic.

Functions: buy_stock(user_id, ticker, shares) sell_stock(user_id,
ticker, shares)

Validation: - sufficient funds - sufficient shares - ticker exists

Return success/failure responses.
